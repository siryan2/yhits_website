<div class="card">
	<header class="card__header">
		<i class="card__icon {{iconClass}}"></i>
		<h3 class="card__headline">{{name}}</h3>
	</header>
	<main class="card__content">
		<p>{{teaser}}</p>			
	</main>
	<footer class="card__footer">
		<a href="{{link}}" class="card__link">mehr <i class="fa fa-angle-right"></i></a>
	</footer>
</div>
